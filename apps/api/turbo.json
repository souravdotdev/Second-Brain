{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "env": ["DATABASE_URL", "REDIS_URL", "PORT"],
      "inputs": ["$TURBO_DEFAULT$", ".env", ".env.*"]
    },
    "build": {
      "env": ["DATABASE_URL", "REDIS_URL", "PORT"],
      "inputs": ["$TURBO_DEFAULT$", ".env", ".env.*"]
    }
  }
}
